# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must use LF line endings
*.sh text eol=lf

# Debian package files must use LF line endings
debian/* text eol=lf

# Systemd service files must use LF
*.service text eol=lf
