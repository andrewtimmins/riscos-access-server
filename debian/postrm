#!/bin/bash
set -e

case "$1" in
    remove)
        systemctl daemon-reload
        ;;
    purge)
        # Remove config file on purge
        rm -f /etc/access.conf
        rm -f /etc/polkit-1/rules.d/90-access-server.rules
        systemctl daemon-reload
        ;;
esac

exit 0
